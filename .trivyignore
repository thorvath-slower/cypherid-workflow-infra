# Trivy triaged-exceptions baseline for cypherid-workflow-infra (CZID-264).
# Policy: ACCEPT the inherited HIGH/CRITICAL backlog, hard-fail on NEW only. Do NOT remediate here —
# logged to backlog #294 for fix-forward. IDs use trivy's short misconfig form (no AVD- prefix),
# matching czid-infra's .trivyignore. Established from PR #3's first run.
#
#   AWS-0031  ECR repository tags are mutable
#   AWS-0089  S3 access logging not enabled
#   AWS-0095  SNS topic not encrypted
#   AWS-0096  SQS queue not encrypted
#   AWS-0104  Security group allows unrestricted egress (CRITICAL — review first)
#   AWS-0130  Launch template does not require IMDS token
#   AWS-0132  Bucket not encrypted with a customer-managed key
#   DS-0002   Dockerfile: no non-root USER
#   DS-0015   Dockerfile: 'yum clean all' missing
#   DS-0029   Dockerfile: '--no-install-recommends' missing
AWS-0031
AWS-0089
AWS-0095
AWS-0096
AWS-0104
AWS-0130
AWS-0132
DS-0002
DS-0015
DS-0029
